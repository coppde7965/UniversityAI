# ---------------------------------------------------------------------------
# .calude 資料夾
# ---------------------------------------------------------------------------
.claude/

# ---------------------------------------------------------------------------
# 環境
# ---------------------------------------------------------------------------
.env

# ---------------------------------------------------------------------------
# Moodle 本體
#
# 自 A-08 起放在具名磁碟區 moodlesrc，不會出現在專案目錄下。這一行留著
# 是為了防止有人手動 clone 一份到這裡——版控它只會讓每次 diff 塞滿幾萬個
# 不是我們寫的檔案。我們的產品程式碼在 plugins/ 之下。
# ---------------------------------------------------------------------------
/moodle/

# ---------------------------------------------------------------------------
# 執行期資料
#
# moodledata 會存放老師上傳的課綱原檔，可能含授課教師姓名與聯絡方式
# （見 SRS 的 OI-03 去識別化說明），絕對不得進版控。
# ---------------------------------------------------------------------------
/moodledata/
/behatdata/
/phpunitdata/

# ---------------------------------------------------------------------------
# 資料庫傾印
#
# 目錄本身要在（bind mount 的掛載點），內容不要——傾印含全部站台資料。
# ---------------------------------------------------------------------------
/db/dumps/*
!/db/dumps/.gitkeep

# ---------------------------------------------------------------------------
# 評測工具鏈
#
# 測試課綱與標註檔含真實課程資料，不進版控（OI-03）。
# uv.lock 要進版控——正確率量測的結果必須可重現（SRS §2.5）。
# ---------------------------------------------------------------------------
/eval/corpus/
/eval/annotations/
/eval/results/
/eval/.venv/
__pycache__/
*.py[cod]

# ---------------------------------------------------------------------------
# 其他
# ---------------------------------------------------------------------------
*.log

# ---------------------------------------------------------------------------
# 編輯器與作業系統
# ---------------------------------------------------------------------------
/.vs/
.vscode/*
!.vscode/extensions.json
.idea/
Thumbs.db
.DS_Store
*.bak
